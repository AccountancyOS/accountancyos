import type { PortalTask } from "../types";

// TODO(batch-2): map to job_tasks / client_tasks scoped to the portal user.
export async function listPortalTasks(): Promise<PortalTask[]> {
  return [];
}